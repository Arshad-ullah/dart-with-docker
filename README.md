A sample command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.


# Variables in bash
- bash variable by default have no data type ..it's means can store value in variables without data type
- it's divide in two cateogry 
1: system define data type
2: user define data type

variable structure:
var_name= value

var_name=$(command) this method is use for to store built-in command output in a variables like:

- directly_path=$(pwd)

# constant variables
- define once and use everywhere


# Arrays in shell script


